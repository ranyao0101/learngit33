# C语言回文数判断程序

## 功能介绍
该程序用于判断输入的整数是否为**回文数**：
- 回文数定义：正读和反读完全相同的整数（例如 121、12321、0）；
- 负数因包含负号，不属于回文数；
- 非0但末尾为0的数（如 120、100），反转后首位为0，不属于回文数。

## 编译与运行
### 环境要求
- 支持C99及以上标准的编译器（如 GCC、Clang、MSVC）；
- 操作系统：Windows/Linux/macOS。

### 编译命令
#### Linux/macOS
```bash
gcc palindrome.c -o palindrome -std=c99